import { ChangeEvent, FC } from "react";
import "./uploader.scss";

const DataMapper: FC<TransformToolProps> = (props) => {

  return <div tabIndex={0} className="rdm-wrapper"></div>;
};

export default DataMapper;
